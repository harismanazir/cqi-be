# Core web frameworks
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
flask>=2.3.0
pydantic>=2.0.0
python-multipart>=0.0.6

# AI and ML packages
langchain-groq>=0.1.0
langchain-core>=0.1.0
langgraph>=0.1.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Environment and configuration
python-dotenv>=1.0.0

# Git integration
GitPython>=3.1.0
PyGithub>=1.59.0

# Production dependencies
gunicorn>=21.2.0

# Standard library enhancements
typing-extensions>=4.0.0

# Note: The following packages are part of Python standard library:
# os, sys, time, json, re, abc, dataclasses, pathlib, asyncio, argparse
# tempfile, shutil, datetime, zipfile, uuid, subprocess, sqlite3, pickle
# hashlib, threading, collections, concurrent.futures, operator