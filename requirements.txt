# Core web frameworks - lightweight versions
fastapi==0.104.1
uvicorn[standard]==0.24.0
flask==2.3.3
pydantic==2.5.0
python-multipart==0.0.6

# Environment and configuration
python-dotenv==1.0.0

# Git integration - fixed versions
GitPython==3.1.40
PyGithub==1.59.1

# Production server
gunicorn==21.2.0

# Standard library enhancements
typing-extensions==4.8.0

# AI/ML packages - CPU-optimized versions
langchain-core==0.1.23
langchain-groq==0.1.3
langgraph==0.1.1

# Lightweight ML packages
numpy==1.24.4
scikit-learn==1.3.2

# Sentence transformers - use CPU-only version to reduce memory
sentence-transformers==2.2.2

# Optional: Add these if you need PyTorch but want CPU-only lightweight version
# torch==2.1.0+cpu -f https://download.pytorch.org/whl/cpu/torch_stable.html
# torchvision==0.16.0+cpu -f https://download.pytorch.org/whl/cpu/torch_stable.html

# Note: Removed heavy CUDA/GPU dependencies to reduce build memory usage
# If you need GPU support, consider using a pre-built image with CUDA pre-installed